<Window x:Class="Blueprint.App.ProgramiWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Tag="FORM_PROGRAMI"
        Title="Programs" Height="500" Width="700"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource FlatBtn}"/>
    </Window.Resources>
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="dgProgrami" Grid.Row="0"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  IsReadOnly="True"
                  SelectionChanged="dgProgrami_SelectionChanged"
                  MouseDoubleClick="dgProgrami_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="[HDR_ID]"             Binding="{Binding Idprograma}"   Width="50"/>
                <DataGridTextColumn Header="[HDR_NAZIV_PROGRAMA]" Binding="{Binding Nazivprograma}" Width="*"/>
                <DataGridTextColumn Header="[HDR_VERZIJA]"        Binding="{Binding Verzija}"       Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <GroupBox Grid.Row="1" Header="Add / Edit" Tag="GRP_UNOS_IZMJENA" Margin="0,6,0,6">
            <Grid Margin="6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>

                <Label Content="Program name:" Tag="LBL_NAZIV_PROGRAMA" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox x:Name="txtNaziv" Grid.Column="1" Margin="0,3,6,3" Padding="3"/>

                <Label Content="Version:" Tag="LBL_VERZIJA" Grid.Column="2" VerticalAlignment="Center"/>
                <TextBox x:Name="txtVerzija" Grid.Column="3" Margin="0,3,0,3" Padding="3"/>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="New"       Tag="CMD_NOVI"       Width="80" Margin="4,0" Click="BtnNovi_Click"    Background="#1E3A5F"/>
            <Button Content="Save"      Tag="CMD_SACUVAJ"    Width="80" Margin="4,0" Click="BtnSacuvaj_Click" Background="#15803D"/>
            <Button Content="Delete"    Tag="CMD_OBRISI"     Width="80" Margin="4,0" Click="BtnObrisi_Click"  Background="#DC2626"/>
            <Button Content="Tables →"  Tag="CMD_TABELE_NAV" Width="90" Margin="4,0" Click="BtnTabele_Click"  Background="#2563EB"/>
            <Button Content="Close"     Tag="CMD_ZATVORI"    Width="80" Margin="4,0" Click="BtnZatvori_Click" Background="#475569"/>
        </StackPanel>
    </Grid>
</Window>
